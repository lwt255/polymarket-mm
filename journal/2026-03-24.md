# 2026-03-24

## Session 1: Liquidity Regime Metrics

### Late One-Sidedness Was Quantified

Built a dedicated liquidity-regime analysis layer on top of the collector data to answer whether late one-sided markets are the thorn in the strategy.

New analysis script:
- `src/scripts/liquidity-regime-analysis.ts`

Shared helper updates:
- `src/scripts/pricing-data-utils.ts`

The analysis now measures:
- first tradable quote time
- first one-sided quote time
- `T-120` / `T-90` / `T-60` market state (`two-sided`, `one-sided`, `not-tradable`)
- T-120 strategy performance conditioned on collapse timing

Key results from the existing raw corpus (`pricing-data.raw.jsonl` at the time of analysis):
- `3,389` raw records loaded
- `3,386` resolved markets
- `3,109` strategy-usable T-120 markets

State at key windows:
- `T-120`: **76.6% two-sided**, **15.2% one-sided**, **8.2% not tradable**
- `T-90`: **70.8% two-sided**, **22.1% one-sided**, **7.1% not tradable**
- `T-60`: **60.9% two-sided**, **32.2% one-sided**, **7.0% not tradable**

Collapse timing split:
- **16.4%** already one-sided by `T-120`
- **7.4%** collapse between `T-120` and `T-90`
- **12.8%** collapse between `T-90` and `T-60`
- **34.6%** still two-sided through `T-60`
- **28.9%** never one-sided in the sampled window

### The Healthy Regime Is Clear

The strategy looks materially healthier in markets that remain two-sided longer.

T-120 strategy conditioning:
- **Still two-sided through T-60**: `692W / 1230L (36.0%)` | **+$21.63/trade** | **43.3% ROI** | avg ask `28.7¢`
- **Already one-sided by T-120**: `15W / 537L (2.7%)` | **+$5.00/trade** | **10.0% ROI** | avg ask `1.8¢`
- **Collapse between T-120 and T-90**: `17W / 206L (7.6%)` | **+$5.34/trade** | **10.7% ROI** | avg ask `7.0¢`
- **Collapse between T-90 and T-60**: `57W / 355L (13.8%)` | **+$11.89/trade** | **23.8% ROI** | avg ask `16.3¢`

Interpretation:
- One-sided-late markets are not the same regime as healthy two-sided markets
- They may still show nominal positive ROI in the simplified sim because prices are tiny, but they are clearly a much weaker and more fragile regime
- The best default filter direction is likely **favor markets that remain two-sided through at least T-60**

### Collector Enriched With Seven New Data Categories

Expanded the live collector to persist the additional market-structure data requested.

`src/scripts/pricing-collector.ts` now records:
1. **Book shape**
   - top 3 bid/ask levels per side
   - cumulative depth within `1¢`, `2¢`, and `5¢`
   - best-depth at underdog ask and favorite bid
2. **Slippage at size**
   - execution estimates for `$10`, `$25`, `$50`, `$100`
   - buy/sell average price, fill ratio, worst price, effective spread
3. **One-sided lifecycle**
   - two-sided / one-sided / untradable ratios
   - first and last times for each state
   - transition counts and one-sided reopen count
4. **Quote stability / churn**
   - quote changes
   - spread changes
   - underdog-side flips
   - largest underdog ask / favorite bid / favorite implied jumps
5. **Underlying price path**
   - running high/low
   - short-horizon moves (`30s`, `60s`, `120s`)
   - realized volatility
   - reversal-after-T120 flag
6. **Snapshot freshness / collection quality**
   - fetch latency
   - quote age from CLOB `timestamp`
   - capture delay vs target snapshot time
   - missed snapshot reasons
7. **Explicit regime labels**
   - `T-120` / `T-90` / `T-60` state
   - first tradable bucket
   - first one-sided bucket
   - collapse-before-T120 / collapse-before-T60 flags

Important limitation:
- The new book-shape, slippage, freshness, and missed-snapshot fields are only available going forward from the collector restart
- Older rows do not contain full ladders or quote timestamps, so those parts cannot be backfilled

### Live Collector Restarted On Enriched Schema

Type-check and smoke checks passed:
- `npx tsc --noEmit --pretty false --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck src/scripts/pricing-data-utils.ts src/scripts/liquidity-regime-analysis.ts src/scripts/pricing-collector.ts`
- `npx tsx src/scripts/liquidity-regime-analysis.ts`
- `npx tsx src/scripts/pricing-collector.ts --duration 0`

After verification:
- restarted the live collector on the enriched schema
- confirmed startup banner at **3:15 PM EDT**
- confirmed a fresh 5-minute batch began immediately under the new code

### Commit Policy Preference Clarified

User explicitly clarified a repo preference:
- when changes are verified and scoped, prefer **commit now** over leaving local changes hanging
- it is less frustrating to revert a clean commit than to let git state and the live process drift apart

That preference is especially important for collector changes, where the running process can diverge from git very easily if code is left uncommitted.

### Git Commits
- `53aeddc` — feat: add liquidity regime analysis
- `1c5a9b4` — feat: enrich collector market microstructure data

### Key Insights

1. **Late one-sidedness is a real regime boundary** — it does not kill the strategy by itself, but it sharply separates weaker markets from the healthier two-sided core.
2. **Raw collection got more valuable** — now that book shape, execution-at-size, freshness, and churn are captured, future analysis can attack microstructure questions directly instead of inferring them from top-of-book alone.
3. **Commit discipline matters operationally** — collector work is unusually prone to git/runtime drift, so verified changes should usually be committed immediately.
