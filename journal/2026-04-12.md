# Polymarket-MM Journal - 2026-04-12

## Quick Stats
- **Status**: Maintenance — context optimization
- **Focus**: CLAUDE.md cleanup for token efficiency
- **Timezone**: All entries UTC

---

## Session Log

### Session 1 — CLAUDE.md cleanup

Applied the same cleanup process used on hyperliquid-mm repo (`docs/claude-md-cleanup-guide.md`). Goal: minimize per-message context cost since CLAUDE.md loads into every message of every conversation.

**What changed**:
- CLAUDE.md: 1,876 words → 270 words (86% reduction)
- AGENTS.md: auto-mirrored
- Created `docs/vps-commands.md` — extracted 40-line command block from CLAUDE.md

**What was cut**:
- Documentation Index (50+ script paths) — discoverable via filesystem
- Tech Stack & Directory Structure — visible from package.json/tsconfig
- Polymarket API Quick Reference — readable from code
- Development Workflow — standard npm commands
- Environment Variables — `.env.example` exists
- AI Agent Guidelines — vague, some contradicted current state
- Performance Tracking — historical stats that don't change behavior

**What was kept**:
- Safety rules (never delete ledgers, Codex review, dry run, verify data span)
- Current state summary (compact)
- VPS service roster (compressed to table)
- Key doc pointers
- Secrets config

### Session 2 — Bot review + trailing stop adjustment

Reviewed the v4 bot's last session (stopped 19:46 UTC after ~21h):
- **236 trades**: 141W/95L, 60% WR, PnL -$1.17 (basically flat)
- Peaked at +$70.22, then gave back gains — trailing stop triggered at $224.18 (floor $225.52)
- Max peak-to-trough drawdown in session: only $26 — barely over the $25 trail

**Post-stop sim**: ran v4 sim on collector data after the bot stopped. The bot missed **26 trades, 73% WR, +$40.07**. The trailing stop cost money this time.

**Trailing stop widened $25 → $75**: The $25 trail was catching normal variance, not edge decay. Worst drawdown seen across all sessions is ~$60, which matches expected standard deviation. New $75 trail gives room for normal swings while still protecting against genuine collapse. `--max-loss 60` remains as absolute floor.

Updated both VPS service file and local `systemd/polymarket-v4-bot.service`.

**Bot restarted** at 02:33 UTC with `--trail 75`.

---

## Key Decisions
- VPS commands moved to `docs/vps-commands.md` rather than cut entirely — still useful as reference, just not needed every conversation
- Trailing stop widened from $25 to $75 — $25 was stopping healthy sessions during normal variance; $60 max observed drawdown is within expected std dev
